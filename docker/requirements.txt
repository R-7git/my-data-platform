# --- DEVOPS TOOLS ---
ansible>=2.9.0
ansible-core>=2.11.0
cffi
cryptography>=3.4.0
Jinja2>=3.0.0
MarkupSafe
packaging
pycparser
PyYAML
resolvelib

# --- AIRFLOW PROVIDERS ---
boto3
faker
apache-airflow-providers-snowflake

# --- THE FIX: PIN DBT VERSIONS ---
# This forces Kubernetes to use the same version as your VS Code
dbt-core>=1.10.0
dbt-snowflake>=1.10.0
