name: 'my_transformation'
version: '1.0.0'
config-version: 2

profile: 'snowflake_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  my_transformation:
    +materialized: table
    +snowflake_warehouse: COMPUTE_WH_PROD
    
    staging:
      +materialized: view
